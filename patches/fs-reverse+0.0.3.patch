diff --git a/node_modules/fs-reverse/index.js b/node_modules/fs-reverse/index.js
index af8541d..d29bbeb 100644
--- a/node_modules/fs-reverse/index.js
+++ b/node_modules/fs-reverse/index.js
@@ -38,7 +38,7 @@ module.exports = function (file, opts) {
     function read () {
       if(stream.destroyed) return
       var length = Math.min(bufferSize, position)
-      var b = new Buffer(length)
+      var b = Buffer.alloc(length)
       position = position - length
       fs.read(fd, b, 0, length, position, function (err) {
         if(err) return onError(err)
